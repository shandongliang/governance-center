// 此文件供应用系统扩展；如下两个方法的方法名及参数不可修改，函数返回值为布尔值，true:显示按钮，false:不显示按钮

// 转办按钮是否显示的逻辑；
const isShowTransferBtn = (businessKey, taskDefinitionKey, procDefinitionKey) => {

}

// 委派按钮是否显示的逻辑；
const isShowDelegateBtn = (businessKey, taskDefinitionKey, procDefinitionKey) => {

}

export {
    isShowTransferBtn,
    isShowDelegateBtn
};
